/* how does compilation work in c
1. pre processing -- removal of comments ,expansion of macros ,expn of included files
2. compilation -- assembly level instruction generate hote hein,.obj file banti
3. Assembly -- .exe banti hai assembly level code machine code mein badalta ,printf resolve nahi hota avhi
4. linking -- resolve the function class, likning the implementations

*/

/* example of pre processing commands
#define -creat a macro
#include- include files
#ifdef- if macro is defined or not
#undef- used to undefine macro
#if-
#else
#elif
#pragma-- to issue some special commands to compiler
#endif--to end 
*/
