/* pointer in c
1.null pointer =Whose value we define to 0 conciously
2.void pointer=generic pointer who don't have a specific data type, used to store the address of any variable

3. wild pointer= a pointer which is not initilised to any varibale address

*/
// There are similiar entity in c called union  it is almost same as structures but the difference are following
// 1. union uses memory more efficenty but can't handle all members at same time
// 2. union uses keyword union
//3. structure allocate different diffent memory locations to his data type
//4. union allocate max size data type equal memory to union child.

#include<stdio.h>
int main(){

    return 0;
}