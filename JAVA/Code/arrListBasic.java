public class arrListBasic {
    // arrlist uses dynamic array internally to implement and
    // whenever we need to add some element in middle, then we have to creat an array and perform another operations, so it cost O(n) time 
    // insertion - O(n);
    // searching is easy bcz the memeory are contagious 
    // so searching - O(1);

    // And in linked list 
    // insertion - O(1);
    //searching- O(n);
}
